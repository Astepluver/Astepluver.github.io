# 📖 博客日常更新教程（Astepluver）

> 这个博客 100% 由 Markdown 文本驱动。你**永远不需要写代码**，只需要：
> 1. 按下面的模板新建一个文本文件；
> 2. 保存后告诉我「发布」，或自己执行文末的 3 行 Git 命令。

---

## 一、项目地图（不用记，遇到再说）

```
src/content/feed/     ← 学习动态：一个 .md 文件 = 一条动态
src/content/papers/   ← 文献库：一个 .md 文件 = 一张文献卡
public/files/papers/  ← 你的 PDF 放这里（暂未创建，用到时建）
public/images/        ← 你的配图放这里（暂未创建，用到时建）
src/consts.ts         ← 站名、副标题、导航标签
src/pages/about.astro ← 「关于」页文字
```

---

## 二、发一条「学习动态」

在 `src/content/feed/` 里**新建文件**，命名为：`日期-简短标题.md`（如 `2026-09-15-耐药文献速览.md`），内容如下：

```markdown
---
date: 2026-09-15          # 必填：YYYY-MM-DD
kind: 文献速览            # 必填：学习打卡 / 文献速览 / 实验随想 / 科研进展 / 随想
tags: [耐药, 结核]        # 可选：任意标签
title: 标题               # 可选：不写则像朋友圈纯文字
image: /images/xxx.jpg    # 可选：配图
draft: false              # 可选：true = 草稿（不显示）
---
今天读了……（正文，写多少都行）
```

> 动态按日期自动倒序排列；文件里的 `title` 可选——不写 title 就是纯文字朋友圈风格。

---

## 三、加一篇「文献」

在 `src/content/papers/` 里新建文件：`简短英文名.md`，模板：

```markdown
---
title: "文献标题（英文原文）"
journal: "期刊名"
year: 2024
authors: "第一作者, et al."        # 可选
direction: 分枝杆菌耐药             # 建议：分枝杆菌耐药 / 细菌耐药性（其他）/ …
tags: [利福平, WGS]                # 可选，多标签用英文逗号
status: 想读                        # 精读 / 在读 / 想读
oa: true                           # true=开放获取；false=订阅（订阅文献不做公开PDF！）
doi: "10.xxxx/xxxxx"               # DOI 会生成官方「在线阅读」按钮
pdf: /files/papers/xxx.pdf         # 可选：有 PDF 才出现「下载 PDF」按钮
note: 一句话点评（显示在卡片上）
---
（可选）精读笔记正文，支持 Markdown 标题、列表、公式等。
```

### 上传 PDF 的约定
1. 把 PDF 放进 `public/files/papers/`；
2. 文件名建议用**英文/数字**（避免中文名在链接里的编码问题）；
3. 把 `pdf:` 填成 `/files/papers/文件名.pdf`。
4. ⚠️ **版权**：只有 OA 文献能公开挂 PDF；订阅文献请删掉 `pdf:` 行，只留摘要 + DOI。

---

## 四、字段速查 & 常见报错

| 你看到的报错 | 原因与修法 |
|---|---|
| `title: Required` / `year: Required` … | 漏填了必填字段，按模板补齐即可 |
| `Invalid enum value` | `kind` 或 `status` 写成了列表外的词，改用提示的合法值 |
| `Error: Invalid frontmatter` | 文件开头 `---` 或结尾的 `---` 少了/多了，检查一遍 |
| 日期不生效 | `date` 必须 `YYYY-MM-DD` 格式 |

写一半不想被看见？加 `draft: true`。

---

## 五、预览 & 发布

**本地预览（可选）**：让我帮你起本地服务，浏览器打开 `http://localhost:4321` 边写边看。

**发布上线**（第④阶段配置好 GitHub 后，3 行命令搞定）：

```bash
cd C:\Users\29411\kimi
git add -A
git commit -m "更新内容：……"
git push
```

推送后约 1–2 分钟，GitHub 自动构建并更新网站，无需其他操作。

---

## 六、别忘了

- 🔒 **隐私红线**：实验数据、患者/样本信息一律不上传（即使脱敏也谨慎）；
- 📄 **版权**：只公开 OA 文献 PDF；订阅文献给摘要 + DOI 链接；
- ✅ **严谨性**：示例文献的期刊/年份/DOI 请以原文为准（示例卡片已尽量准确，替换前请核对）；
- 💬 遇到任何报错或想改样式，把提示文字发给我即可。
